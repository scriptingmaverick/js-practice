# Pookie Battle

  Multiple users can battle against each other. 

  Needs : 
  1. stats -> stats.slice(0,4)
  2. sprites

  pokemon format: 
  {
    pokeId : id,
    pokemon : string,
    hp: number,
    moves: [
      {
    move_name: string,
    type: attack/defense/evade
    effect: number
  },....]
  }


